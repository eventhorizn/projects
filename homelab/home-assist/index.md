# Setup

Will be a standalone VM with HAOS running on it

Easy mode is to run the following script:

```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/community-scripts/ProxmoxVE/main/vm/haos-vm.sh)"
```
